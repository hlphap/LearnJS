<div class="mt-4 container-fluid">
  <h3>Chính sách của tôi</h3>

  <table class="table mt-4">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Tên chính sách</th>
        <th scope="col">Đơn tối thiểu</th>
        <th scope="col">Đơn tối đa</th>
        <th scope="col">Chiếc khấu</th>
        <th scope="col">Hành động</th>
      </tr>
    </thead>
    <tbody>
      {{#each policy}}
      <tr>
        <th scope="row">{{sum @index 1}}</th>
        <td>{{this.name}}</td>
        <td>{{this.orderPerMonthMin}}</td>
        <td>{{this.orderPerMonthMax}}</td>
        <td>{{this.ratioCommission}}</td>
        <td>{{this.note}}</td>
        <td>
          <a href="/policy/{{this._id}}/edit" class="btn btn-warning">Sửa</a>
          <a href="" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#delete-course-modal"
            data-id="{{this._id}}">Xóa</a>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>

{{! Confirm delete course}}
<div id="delete-course-modal" class="modal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Xóa khóa học?</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <p>Bạn chắc chắn muốn xóa khóa học này?</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
        <button id="btn-delete-course" type="button" class="btn btn-danger">Xóa bỏ</button>
      </div>
    </div>
  </div>
</div>

{{!--Delete hidden Form --}}
<form method="POST" name="delete-course-form"></form>

<script>
  let courseId;
  let deleteForm = document.forms["delete-course-form"];
  let deleteCourseModal = document.getElementById('delete-course-modal');
  let btnDeleteCourse = document.getElementById("btn-delete-course");

  deleteCourseModal.addEventListener('show.bs.modal', function (event) {
    let button = event.relatedTarget;
    courseId = button.getAttribute('data-id');
    console.log(id);
  })

  btnDeleteCourse.onclick = function () {
    deleteForm.action = "/policy/" + courseId + "?_method=DELETE";
    deleteForm.submit();
  }
</script>